services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: node_mps_be
    volumes:
      - ./main.db:/app/main.db
    ports:
      - "8730:3000"
    restart: "unless-stopped"